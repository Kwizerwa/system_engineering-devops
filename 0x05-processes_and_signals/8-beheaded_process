#!/usr/bin/env bash
# This script kills the 7-highlander process using SIGKILL

pids=$(pgrep -f "7-highlander")

if [[ -z "$pids" ]]; then
  echo "No 7-highlander process found."
  exit 1
fi

for pid in $pids; do
  kill -9 "$pid"
done

echo "Sent SIGKILL to 7-highlander process(es): $pids"
