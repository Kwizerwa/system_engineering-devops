#!/usr/bin/env bash
# This script kills the 7-highlander process using SIGKILL and prints only OK

pids=$(pgrep -f "7-highlander")

if [[ -n "$pids" ]]; then
  for pid in $pids; do
    kill -9 "$pid"
  done
fi

echo OK
